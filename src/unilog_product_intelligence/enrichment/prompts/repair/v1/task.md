Review the candidate and validation failures supplied by the application. Return at most one
repaired value when an allowed canonical representation is directly supported by the supplied
evidence and rules; otherwise return review_required with the failure preserved.
